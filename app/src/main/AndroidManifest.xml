<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.francis.touroftheni">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logomakr"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Title" />
        <activity android:name=".Templetot" />
        <activity
            android:name=".TempleListItem"
            android:label="Single Item Selected" />
        <activity android:name=".Hills" />
        <activity android:name=".Dams" />
        <activity android:name=".Falls"></activity>
    </application>

</manifest>